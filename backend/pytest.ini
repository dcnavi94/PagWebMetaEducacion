[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
    --basetemp=../temp_pytest_accessible
    --cov=app --cov-report=term-missing --cov-fail-under=80
    -p no:tmpdir
filterwarnings =
    ignore::DeprecationWarning
